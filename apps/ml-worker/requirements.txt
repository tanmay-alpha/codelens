# =====================================================
# CodeLens — ml-worker runtime dependencies (inference)
# =====================================================
# Used by apps/ml-worker/Dockerfile and local inference runs.
# Training-only extras live in requirements-train.txt.
#
# Versions chosen for reproducibility and to match Section 7
# of ENGINEERING_PLAN.md (training pipeline uses the same pins).
# =====================================================

# --- Web framework ---
fastapi==0.138.0
uvicorn[standard]==0.49.0

# --- ML core (CodeBERT inference) ---
torch==2.12.1
transformers==5.12.1

# --- Numerics ---
numpy==2.4.6

# --- Schemas + config ---
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2
