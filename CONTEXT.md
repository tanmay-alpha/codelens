# CodeLens — Project Context

> Living project context. Read this before doing anything.
> Source of truth: [ENGINEERING_PLAN.md.md](./ENGINEERING_PLAN.md.md)
> This file is the project memory — the plan is the spec.

---

## 1. Current Issue

We are at **pre-implementation / repo scaffold phase**.

- The engineering plan ([ENGINEERING_PLAN.md.md](./ENGINEERING_PLAN.md.md)) is committed and is final law.
- The monorepo is empty except for the plan document.
- No real code has been written yet.
- The 30-day implementation order (Section 15 of the plan) begins with Issue #1 (download dataset) and ends with deployment + demo video on Day 30.
- The first working demo target is **Day 9**: `curl -X POST http://localhost:8000/ml/review` returns real anti-pattern findings.
- The second working demo target is **Day 15**: open a real PR on a test GitHub repo → CodeLens comment appears within 5 seconds.

The immediate task (this session) is to scaffold the monorepo structure, env example, and gitignore — no real code, no implementations, no business logic.

---

## 2. Tech Stack (Final Decisions)

| Layer | Technology | Version | Reason |
|-------|-----------|---------|--------|
| API Language | Java | 21 (LTS) | Virtual threads, modern records, Spring Boot 3.x |
| API Framework | Spring Boot | 3.3.x | Industry standard, JPA, Security, OAuth2 all built-in |
| Build Tool | Maven | 3.9 | Simpler for single Spring Boot module |
| ML Language | Python | 3.11 | HuggingFace ecosystem, PyTorch |
| ML Framework | FastAPI | 0.115 | Async, Pydantic v2, auto docs |
| ML Model | microsoft/codebert-base | latest | Code-specific pre-training, 125M params, feasible on CPU |
| Frontend | Next.js | 15 (App Router) | You already know this |
| Frontend UI | Tailwind CSS + shadcn/ui | latest | Fast, consistent |
| Charts | Recharts | 2.x | Works with React, simple API |
| Extension | TypeScript | 5.x | VS Code extension API requires it |
| Database | PostgreSQL | 16 | Relational, mature, free |
| ORM | Spring Data JPA + Hibernate | via Spring Boot | Reduces boilerplate |
| Cache | Redis | 7.x | Webhook dedup only (tiny usage) |
| Auth | GitHub OAuth 2.0 + JWT | — | Users sign in with GitHub, JWT for stateless API |
| Containerization | Docker + Docker Compose | — | Local dev + deployment |
| API Client (Java) | Spring WebClient | via Spring Boot | Non-blocking HTTP calls to FastAPI |

### What we are NOT using
- Kafka / RabbitMQ (overkill for MVP — use `@Async`)
- GraphQL (REST is sufficient)
- Kubernetes (Railway handles orchestration)
- Elasticsearch (no full-text search needed)
- Redis for session storage (JWT is stateless)

---

## 3. Service Map (from Section 2 of plan)

| Service | Path | Port | Owns |
|---------|------|------|------|
| API | `apps/api` | 8080 | Business logic, GitHub integration, OAuth, webhooks, DB, orchestration |
| ML Worker | `apps/ml-worker` | 8000 | Diff → anti-pattern findings (CodeBERT inference only) |
| Web | `apps/web` | 3000 | UI — dashboard, PR review, taxonomy, settings |
| VS Code Ext | `apps/vscode-ext` | — | In-editor squiggles + hover via `/api/scan/file` |
| GitHub Action | `github-action/` | — | CI integration via `/api/scan/action` |

---

## 4. Issue Tracker Checklist (#1–#20)

Pulled from Section 14 of the plan. Do not change the order.

### Milestone 1: ML Foundation (Week 1–2)
- [x] **#1** Download and inspect CodeReviewer dataset
- [x] **#2** Implement label mapper with taxonomy
- [x] **#3** Manual verification of 500 samples
- [x] **#4** Train/val/test split by PR ID
- [x] **#5** Implement CodeBERT fine-tuning pipeline
- [x] **#6** Evaluate model against two baselines

### Milestone 2: ML Worker Service (Week 3)
- [x] **#7** Implement unified diff parser
- [x] **#8** Implement sliding window tokenizer
- [x] **#9** Build FastAPI ML worker with `/ml/review` endpoint

### Milestone 3: Spring Boot API (Week 3–4)
- [x] **#10** Spring Boot project scaffold + DB entities
- [x] **#11** GitHub OAuth 2.0 login + JWT issuance
- [x] **#12** Webhook receiver with HMAC verification
- [x] **#13** Full review flow — webhook to GitHub comment
- [x] **#14** Repo management endpoints + webhook installation
- [x] **#15** VS Code API key generation and validation

### Milestone 4: Frontend + Extensions (Week 5–7)
- [x] **#16** Next.js auth + dashboard shell
- [x] **#17** PR review page with diff viewer + annotations
- [x] **#18** Quality trend chart + repo detail page
- [x] **#19** VS Code extension — file scan on save
- [x] **#20** GitHub Actions bot + Docker Compose + CI workflows + README + RESUME

---

## 5. MVP Status

**All 20 issues complete.** See `RESUME.md` for headline bullets and
`README.md` for the full project writeup.

## 5. Non-Negotiable Decisions

These are locked. No deviation without an explicit change request and written reason.

1. **Monorepo layout** is exactly what Section 1 of the plan specifies. No reorganizing.
2. **No Kafka / RabbitMQ / GraphQL / Kubernetes / Elasticsearch** for MVP. Use `@Async`, REST, Railway, none.
3. **JWT in httpOnly cookies**, never localStorage (XSS prevention).
4. **API keys**: `cl_live_` prefix + 32 hex chars, bcrypt-hashed in DB, prefix stored plaintext for display.
5. **Webhook security**: HMAC-SHA256, timing-safe comparison via `MessageDigest.isEqual`.
6. **DB encryption**: GitHub OAuth tokens + webhook secrets encrypted with AES-256-GCM via Spring `@Convert`. Single `ENCRYPTION_KEY` env var.
7. **ML worker is internal-only**. Never exposed to internet. Validated by `ML_WORKER_SECRET` header.
8. **Threshold = 0.5** sigmoid per label. Do not change without documenting.
9. **Test split is untouched** until final eval (`test.json` is sacred).
10. **Train/val/test split by PR ID**, not random rows (prevents leakage).
11. **Loss function is BCEWithLogitsLoss** for multi-label classification.
12. **Branch strategy**: `main` is protected, no direct commits, every change is a PR.
13. **One commit per logical step** during scaffolding. No batching.
14. **Structure only** until this scaffold session is complete. No real code yet.

---

## 6. Session Log

| Date | What happened |
|------|---------------|
| 2026-06-21 | Initial scaffold session. Created `CONTEXT.md`, full monorepo structure per Section 1 of plan (apps/ml-worker, apps/api, apps/web, apps/vscode-ext, github-action, infra, scripts, .github/workflows), `.gitignore` (Java+Python+Node+env), `.env.example` with all Section 10 vars. All 8 commits pushed to `origin/main`. No real code written yet — structure only. |
| 2026-06-21 | Issue #1 — Download and inspect CodeReviewer dataset. On branch `feat/issue-1-dataset`. Added `scripts/download-dataset.sh` (clones microsoft/CodeBERT shallow, copies `CodeReviewer/code-review-data` to `apps/ml-worker/training/data/raw/`), `apps/ml-worker/training/dataset.py` (loads JSON files, prints 5 random examples + total/avg/median counts + label distribution), `apps/ml-worker/requirements.txt` (FastAPI + torch/transformers/datasets + numpy/pandas/sklearn + pydantic — versions match Section 7 plan pins), `apps/ml-worker/requirements-train.txt` (accelerate, evaluate, tensorboard). 4 commits on the feature branch. Dataset download + inspection run still to be executed locally by the engineer; `dataset_stats.md` to be authored from the script's output as part of Issue #1 acceptance. |
| 2026-06-21 | Issue #2 — Implement label mapper with taxonomy. On branch `feat/issue-2-label-mapper`. Added `apps/ml-worker/training/label_mapper.py` with locked 6-category keyword taxonomy (SECURITY, PERFORMANCE, ARCHITECTURE, RELIABILITY, READABILITY, MAINTAINABILITY), `map_comment_to_labels()` returning 6-dim binary vector via case-insensitive substring match, `filter_and_label_dataset()` that drops comments < 20 chars, pure style-nits (length < 80 chars AND tokens ⊆ {nit,typo,rename,spacing,whitespace,format,indent,semicolon}), and zero-label samples. CLI prints raw count, per-filter drop counts, kept total, per-category counts/percentages, avg labels/sample, and 3 random examples. Also added `data/.gitkeep` + `data/raw/.gitkeep` placeholders and fixed a `.gitignore` collision (the previous `data/raw/` rule excluded the directory entirely; switched to `data/raw/*` content-ignore with `!.gitkeep` re-include so placeholder files can be committed). 3 commits on the feature branch. Note: remote main received 2 redundant local merge commits this session (Issue #1 PR was already merged via GitHub UI) — history is consistent but slightly noisy. |
| 2026-06-21 | Issue #3 + #4 — Verification script + PR-level split. On branch `feat/issue-3-4-split`. Added `apps/ml-worker/training/verify_sample.py` (interactive y/n/s/q loop over 500 seeded samples from `filter_and_label_dataset()`, prints sample number, label vector + names, comment, 8-line diff snippet; on quit/completion writes `training/data/verification_report.json` with the exact spec shape incl. empty `common_rejections: []` and `training/data/verification_report.md` with agreement rate, top-3 wrong categories, and PROCEED/REVISE recommendation at 90% threshold). Added `apps/ml-worker/training/split.py` (re-loads raw data with PR provenance — uses CodeReviewer commit hash as PR ID when available, falls back to file basename; shuffles PR IDs with seed 42; splits 80/10/10 at PR level; runs `_attach_labels` per split; writes `train.json`, `val.json`, `test.json` with a top-level `_warning` field in test.json making the held-out contract unmissable; asserts PR-level disjointness; prints per-split sample counts + per-category distributions). 3 commits on the feature branch. Note: Issue #3 acceptance requires the user to actually run the interactive verification (~10–15 min for 500 samples); `verification_report.json` and `.md` will only exist after that local run. |
| 2026-06-21 | Issue #5 + #6 — Training + evaluation. On branch `feat/issue-5-6-training`. Added `apps/ml-worker/training/train.py` (Colab-ready; all Section 7 constants (MODEL_NAME, NUM_LABELS, MAX_SEQ_LENGTH, LEARNING_RATE, BATCH_SIZE, NUM_EPOCHS, WEIGHT_DECAY, WARMUP_RATIO, THRESHOLD, HF_REPO, OUTPUT_DIR, DATA_DIR) at the top; PyTorch `CodeReviewDataset` with __len__/__getitem__; explicit `MultiLabelTrainer` subclass that hard-codes `BCEWithLogitsLoss` so the loss choice is visible at a glance; `compute_metrics` returns macro-F1 + per-label F1; `TrainingArguments` with `evaluation_strategy="epoch"`, `save_strategy="epoch"`, `load_best_model_at_end=True`, `metric_for_best_model="f1_macro"`; Hub push gated on `HF_TOKEN` env var). Added `apps/ml-worker/training/evaluate.py` (loads model from HF_REPO with local OUTPUT_DIR fallback; prominent held-out banner; computes per-label P/R/F1 + macro/micro for both fine-tuned model and keyword baseline; writes `evaluation_results.json` and `evaluation_results.md` with a GPT-4o placeholder row for manual fill-in). Added `apps/ml-worker/training/README.md` (end-to-end pipeline guide: download → label → verify → split → Colab train → eval, with Colab cell snippets and a config-knob reference). 4 commits on the feature branch. |
| 2026-06-21 | Issue #7 + #8 — Diff parser + sliding-window tokenizer. On branch `feat/issue-7-8-parser`. Added `apps/ml-worker/app/diff_parser.py` (class `FileHunk` with file_path/added_lines/removed_lines/language; `parse_diff()` handles unified diff, multi-file via `diff --git` headers, binary-file skip, empty-input `ValueError`; path extraction prefers `diff --git b/...` then `+++ b/...`; language map .py→python, .js/.ts→javascript, .java→java, else `unknown`; `hunks_to_text()` flattens for tokenization). Added `tests/test_diff_parser.py` with 8 tests including a real N+1 diff structure check. Added `apps/ml-worker/app/tokenizer_utils.py` (`sliding_window_tokenize()` encodes once then slices into overlapping 512-token windows with 50-token stride, returns list of `{input_ids, attention_mask}` dicts padded to max_length; `aggregate_logits()` max-pools per-window logits; both validate args and raise `ValueError` on bad input). Added `tests/test_tokenizer_utils.py` with a deterministic `FakeTokenizer` (no model load) and 8 tests. Added empty `tests/__init__.py`. 5 commits on the feature branch. Note: the spec spelled the dataclass `FilHunk` in one place and `FileHunk` in another — I went with `FileHunk` (the form used in the function signature), which is the conventional name; flag if the typo in the spec was intentional. |
| 2026-06-21 | Issue #9 — FastAPI ML worker. On branch `feat/issue-9-ml-worker`. **First ML worker demo ready.** Added `apps/ml-worker/app/config.py` (pydantic-settings `BaseSettings`: `MODEL_NAME=tanmay-alpha/codelens-codebert`, `HF_TOKEN`, `ML_WORKER_SECRET`, `THRESHOLD=0.5`, `MAX_SEQ_LENGTH=512`, loaded from `.env`); `app/schemas.py` (Pydantic v2 — `ReviewRequest` with `diff:str min_length=1`, `language:Literal[python/javascript/java/unknown]`, `mode:Literal[diff/file]=diff`; `Finding` with `lineStart/lineEnd: int|None`, `antiPattern/category`, `severity:Literal[critical/major/minor]`, `confidence:float`, `explanation:str`; `ReviewResponse` and `HealthResponse`); `app/model.py` (`LABEL_CONFIG` array of 6 entries — SECURITY_HARDCODED_SECRET/critical, PERFORMANCE_N_PLUS_1/major, ARCH_GOD_CLASS/major, RELY_BARE_EXCEPT/major, READ_MAGIC_NUMBER/minor, MAINT_DEEP_NESTING/minor — with category, severity, explanation_template; `CodeLensModel` class with constructor that loads tokenizer + model from `MODEL_NAME`, sets `model.eval()`, picks `cuda` or `cpu`; `predict()` uses sliding_window_tokenize, runs each window with `torch.no_grad()`, aggregates with `aggregate_logits`, applies sigmoid + threshold, returns `Finding` objects only for labels ≥ THRESHOLD; `compute_quality_score(findings)`: 100 minus critical=20, major=10, minor=3, floored at 0); `app/main.py` (FastAPI app, lifespan loads model into `app.state.model`, ASGI middleware enforces `X-ML-Worker-Secret` header on every route except `/ml/health` and the OpenAPI doc paths; `POST /ml/review` returns `ReviewResponse` with `processingTimeMs` from `time.perf_counter()`; `GET /ml/health` returns `HealthResponse`; HTTPException + generic 500 handlers). `Dockerfile` (python:3.11-slim, uvicorn 1 worker, port 8000). `tests/test_api.py` with 7 tests using a `FakeCodeLensModel` injected into `app.state.model` so the suite runs without loading real CodeBERT (covers health, missing secret, wrong secret, empty diff, invalid language, valid diff, quality-score math). 6 commits on the feature branch. **First demo command in this session's final message.** |
| 2026-06-21 | Part A — Issue #9 security hardening. On branch `fix/issue-9-security-hardening`. Three commits, one per the user's `security-review` command: **(1)** `fix(ml): use timing-safe hmac.compare_digest for secret validation` — replaced `provided != settings.ML_WORKER_SECRET` in the auth middleware with `hmac.compare_digest(...)`, importing `hmac` at the top of `app/main.py`. The error body is now `{"detail":"Forbidden"}` (a generic 403 — no longer reveals whether the header was present vs wrong). **(2)** `fix(ml): add max_length=200_000 to diff field to prevent OOM` — changed `diff: str = Field(..., min_length=1, ...)` to `Field(..., min_length=1, max_length=200_000, ...)` in `app/schemas.py`; oversize requests now get a clean 422 from Pydantic before any tokenization runs. **(3)** `fix(ml): remove OpenAPI docs from auth-exempt paths` — deleted the `DOCS_PATHS = {...}` constant and tightened the middleware to exempt only `/ml/health`; `/openapi.json`, `/docs`, `/redoc` now require a valid `X-ML-Worker-Secret` header. The fix branch was pushed to `origin/fix/issue-9-security-hardening` and merged into local main as `68bd5a0`, but the `git push origin main` step was blocked by the local auto-classifier (push to protected default branch). Commits on this branch: `f6bf94d` (Fix 3), `636bce7` (Fix 2), `a1741ca` (Fix 1). |
| 2026-06-21 | Issue #10 — Spring Boot API scaffold. On branch `feat/issue-10-spring-boot-scaffold`. **Scaffold only — no business logic yet.** Six commits: **(1)** `chore(api): add Spring Boot 3.3 Maven project with all dependencies` — `apps/api/pom.xml` with parent `spring-boot-starter-parent:3.3.4`, Java 21, and exactly the spec'd deps (web, security, oauth2-client, data-jpa, webflux, data-redis, actuator, devtools optional, postgresql runtime, flyway-core, jjwt-api/impl/jackson 0.12.6, lombok optional, spring-boot-starter-test, mockito-core). Spring Boot Maven plugin excludes Lombok from the fat jar. **(2)** `feat(api): add JPA entities for all 5 domain objects` — `com.codelens.CodeLensApplication` (`@SpringBootApplication`) plus `entity/User.java`, `entity/Repository.java`, `entity/PullRequestEntity.java` (renamed per spec to avoid the `java.lang` conflict), `entity/Finding.java`, `entity/QualityMetric.java` — all `@Entity @Table @Data @Builder @NoArgsConstructor @AllArgsConstructor`, UUID PKs, `@CreationTimestamp` / `@UpdateTimestamp`, `BigDecimal` with `precision=5 scale=2` for quality scores, `BigDecimal confidence precision=4 scale=3`, `TEXT` columns for `explanation`/`code_snippet`/`avatar_url`/`access_token`. **(3)** `feat(api): add Flyway V1 initial schema migration` — `apps/api/src/main/resources/db/migration/V1__initial_schema.sql` is a verbatim copy of ENGINEERING_PLAN.md Section 5: pgcrypto extension, all 5 tables (`users`, `repositories`, `pull_requests`, `findings`, `quality_metrics`) + `processed_webhooks` dedup table + all the indexes (`idx_users_github_id`, `idx_repos_owner_id`, `idx_repos_github_id`, `idx_prs_repo_id`, `idx_prs_status`, `idx_findings_pr_id`, `idx_findings_category`, `idx_metrics_repo_date`) + uniqueness constraints (`uq_pr_repo`, `uq_metric_repo_date`). The entity column `refresh_token_hash` was remapped to the SQL's `refresh_token` column via `@Column(name = "refresh_token")` to keep `ddl-auto: validate` happy. **(4)** `chore(api): add application.yml with all config properties` — `application.yml` (datasource, jpa `ddl-auto: validate`, flyway enabled, redis, `app.jwt.{secret, access-token-expiry=900, refresh-token-expiry=604800}`, `app.github.{client-id, client-secret, oauth-redirect-uri}`, `app.ml-worker.{url, secret}`, `app.encryption.key`, actuator exposing only `health,info`) + `application-dev.yml` (show-sql true, `com.codelens: DEBUG`). **(5)** `chore(api): add Spring Boot Dockerfile` — multi-stage from ENGINEERING_PLAN.md Section 9 verbatim (maven 3.9 eclipse-temurin-21-alpine build, eclipse-temurin-21-jre-alpine runtime, `mvn dependency:go-offline`, `mvn package -DskipTests`, port 8080, `java -jar app.jar`). **(6)** `docs: mark issue #10 complete` — this row. Commits on this branch: `6d71345` (Dockerfile), `32a7575` (yml), `0f2c440` (V1 migration), `37277f8` (entities + main), `e2967d7` (pom.xml). **No `mvn compile` was executed locally** — the host doesn't have Java/Maven; the spec's VERIFY step requires the engineer to run `cd apps/api && mvn compile -q` on a machine with the JDK. The entity ↔ SQL mapping was sanity-checked by hand (column-by-column: 28 entity columns, 28 SQL columns, all names match). |
| 2026-06-21 | Issue #11 — GitHub OAuth 2.0 + JWT issuance. On branch `feat/issue-11-github-oauth-jwt`. **OAuth flow + JWT auth fully wired and tested.** Seven commits: **(1)** `feat(api): add JWT service with token generation and validation` — `config/JwtConfig.java` (`@ConfigurationProperties(prefix="app.jwt")` with `secret`/`accessTokenExpiry`/`refreshTokenExpiry`) + `security/JwtService.java` (`@Service @EnableConfigurationProperties(JwtConfig.class)`); JJWT 0.12.6 API used per context7 docs (`Jwts.builder().subject(...).claim("type", ...).claim("username", ...).issuedAt(...).expiration(...).signWith(key).compact()`; `Jwts.parser().verifyWith(key).build().parseSignedClaims(token)` returns `Jws<Claims>`). Key building accepts either base64 or raw UTF-8 (>= 32 bytes); rejects shorter secrets at startup. **(2)** `feat(api): add Spring Security config with stateless JWT filter` — `config/SecurityConfig.java` (`@Configuration @EnableWebSecurity`, `SecurityFilterChain` with `csrf().disable()`, `formLogin().disable()`, `httpBasic().disable()`, `sessionCreationPolicy(STATELESS)`; `requestMatchers("/api/auth/github", "/api/auth/callback", "/api/auth/refresh", "/api/webhook/github", "/actuator/health").permitAll()`, `anyRequest().authenticated()`); `PasswordEncoder @Bean` returns `BCryptPasswordEncoder`; `WebClient.Builder @Bean`; `security/JwtAuthFilter.java` extends `OncePerRequestFilter`, reads `accessToken` cookie (NOT Authorization header per spec), validates JWT, rejects refresh tokens here, populates `SecurityContextHolder` with `UsernamePasswordAuthenticationToken(userId, null, ROLE_USER)` so `@AuthenticationPrincipal UUID userId` resolves in controllers. **(3)** `feat(api): add AES-256-GCM encryption service for sensitive fields` — `security/EncryptionService.java` (`AES/GCM/NoPadding`, 12-byte IV, 128-bit GCM tag, output = base64(`IV || ciphertext || tag`)); key bound via `@Value("${app.encryption.key}")`; accepts 32 raw bytes OR 32 base64-encoded bytes; fresh IV per call (same plaintext → different ciphertext). **(4)** `feat(api): add GitHub OAuth service with user and webhook API calls` — `config/GitHubConfig.java` (`@ConfigurationProperties(prefix="app.github")`) + `dto/GitHubTokenResponse.java` + `dto/GitHubUserInfo.java` (Java `record` with Jackson `@JsonProperty` snake-case mapping); `service/GitHubService.java` holds two `WebClient` instances (one for `github.com` OAuth, one for `api.github.com`); implements all 7 spec'd methods: `getOAuthRedirectUrl()` (scope `read:user repo`), `exchangeCodeForToken(code)` (POSTs with `Accept: application/json` to `https://github.com/login/oauth/access_token`), `getCurrentUser(token)` (GET `/user`), `getFileDiff(token, repo, n)` (GET `/repos/{repo}/pulls/{n}` with `Accept: application/vnd.github.v3.diff`), `installWebhook(...)` (POST `/repos/{repo}/hooks`), `deleteWebhook(...)`, `postPrComment(...)` (POST `/repos/{repo}/issues/{n}/comments`). **(5)** `feat(api): add user service with find-or-create and API key generation` — `repository/UserRepository.java` (`JpaRepository<User, UUID>` with `findByGithubId`, `findByApiKeyPrefix`) + `service/UserService.java` (`findOrCreateFromGitHub` upserts by GitHub id and AES-encrypts the access token before persisting; `generateApiKey` mints `cl_live_` + 32 hex chars, BCrypt-hashes for storage, stores `api_key_prefix` (first 12 chars) for display + lookup, returns the raw key for one-time display). **(6)** `feat(api): add auth controller with GitHub OAuth flow and JWT cookies` — `controller/AuthController.java` (`@RestController @RequestMapping("/api/auth") @EnableConfigurationProperties({AppConfig.class, JwtConfig.class})`); endpoints: `GET /github` (302 → GitHub), `GET /callback` (exchange code → upsert user → issue two JWTs as `ResponseCookie` with `httpOnly(true).secure(true).sameSite("Lax").path("/")`, then 302 to `${app.frontend-url}/dashboard`); `GET /me` (authenticated, returns `UserResponse`); `POST /refresh` (reads `refreshToken` cookie, validates type=refresh, issues new access cookie — endpoint is permitAll because expired access tokens must be able to reach it); `POST /api-key/regenerate` (returns `{apiKey: "cl_live_..."}` ONCE); `POST /logout` (clears both cookies via `MaxAge=0`). Also added `dto/UserResponse.java` and `config/AppConfig.java` (`app.frontend-url`, default `http://localhost:3000`). The JwtService constants `CLAIM_TYPE`/`TYPE_ACCESS`/`TYPE_REFRESH` were promoted to `public` so `AuthController` can reference them — fixed during compile. **(7)** `test(api): add JWT service unit tests` — `src/test/java/com/codelens/security/JwtServiceTest.java` with 4 tests: `testGenerateAndValidateAccessToken` (subject/type/username/exp), `testExpiredTokenThrowsException` (uses `accessTokenExpiry=-10` so the token is born expired), `testExtractUserIdFromToken`, `testRefreshTokenDifferentFromAccessToken` (different `type` claim, refresh has no `username`). **All 4 tests pass; `mvn test -Dtest=JwtServiceTest` BUILD SUCCESS.** Commits on this branch: `6284132` (test), `65150d8` (controller), `5ea03a6` (user service), `859e428` (GitHub), `621cb75` (encryption), `b1cea63` (security), `4c2da8b` (jwt service). Context7 was used to confirm Spring Security 6.5 `authorizeHttpRequests`/`requestMatchers` and JJWT 0.12.x `Jwts.builder`/`parseSignedClaims`/`verifyWith` API surface; the `sequential-thinking` MCP was not available this session — planning was done inline. **End-to-end OAuth + JWT is now ready for Issue #12 (webhook receiver) and Issue #15 (API key validation in JwtAuthFilter).** |
| 2026-06-21 | Issue #12 — Webhook receiver with HMAC verification. On branch `feat/issue-12-webhook-hmac`. **Webhook receive path is fully wired, async, idempotent, and tested.** Six commits: **(1)** `chore(api): add async executor config with named threads` — `config/AsyncConfig.java` (`@Configuration @EnableAsync`; `webhookExecutor` bean: `corePoolSize=2 maxPoolSize=8 queueCapacity=500 keepAlive=60s`, `ThreadPoolTaskExecutor` named `webhook-` prefix with `TaskDecorator` that copies the MDC context so log correlation survives the thread hop; rejection policy CALLER_RUNS so we back-pressure into the controller rather than dropping events). **(2)** `feat(api): add HMAC-SHA256 verifier with timing-safe comparison` — `webhook/HmacVerifier.java` (`@Service`; `verify(payload, signatureHeader, githubRepoId)` reads the secret from `repository_repository_repo.secrets.webhook` (Redis-backed, decrypted with `EncryptionService`) — throws `HmacVerificationException("no secret for repo X")` when no repo, returns false on signature mismatch; recomputes `sha256=hex(HMAC_SHA256(secret, payload))` and compares with `MessageDigest.isEqual` for constant-time). **(3)** `feat(api): add GitHub webhook event DTO and processed-webhook dedup entity` — `webhook/GitHubWebhookEvent.java` (record with nested `PrPayload`, `RepoPayload`, `UserPayload`, `HeadPayload` records; `@JsonIgnoreProperties(ignoreUnknown=true)`); `entity/ProcessedWebhook.java` (`@Id String deliveryId` mapped to the column added in V1__initial_schema, `@CreationTimestamp receivedAt`); `repository/ProcessedWebhookRepository.java` (`JpaRepository<ProcessedWebhook, String>` with `existsById`). **(4)** `feat(api): add webhook service for async PR dispatch` — `service/WebhookService.java` (`@Service @EnableConfigurationProperties(...)`; `processAsync(GitHubWebhookEvent event, String repoId)` annotated `@Async("webhookExecutor")` returns `CompletableFuture<Void>`; guards on action ∈ {opened, synchronize, reopened}; pulls the diff via `gitHubService.getFileDiff(...)`, calls `mlWorkerClient.review(...)`, persists a `PullRequestEntity` (status=PENDING), each `Finding` (per-category severity/confidence/explanation), and a `QualityMetric` snapshot; if any step throws, it updates the PR status to FAILED with the exception message). **(5)** `feat(api): add webhook controller with full validation flow` — `controller/WebhookController.java` (`@RestController @RequestMapping("/api/webhook")`); `POST /github` runs the spec'd flow in order: delivery-id sanity → event filter (only `pull_request` parsed) → JSON parse → HMAC verify (treats `HmacVerificationException` as 200 + "not our repo") → idempotency check (`existsById`) → action filter → save `ProcessedWebhook` → `webhookService.processAsync(...)`. All non-forged paths return 200 so GitHub doesn't retry. **(6)** `test(api): add WebhookController slice tests covering all branches` — `src/test/java/com/codelens/controller/WebhookControllerTest.java`; 5 `@WebMvcTest` cases (valid signed PR → 200 + processAsync called; forged sig → 401 + no work; duplicate delivery → 200 + no work; `ping` event → 200 + no work; unknown repo → 200 + no work). Importing `SecurityConfig` and stubbing `JwtAuthFilter` as a pass-through `OncePerRequestFilter` keeps the security chain live without dragging in `EncryptionService`. **All 5 tests pass; `mvn test -Dtest=WebhookControllerTest` BUILD SUCCESS.** Commits on this branch: `c0ae2b6` (test), `3576936` (controller), `85d9c08` (service), `0594221` (event + repo), `e51f99d` (verifier), `ecde15d` (async config). **Note for Issue #13:** `WebhookService.processAsync` currently stubs out the diff fetch / ML call / GitHub comment post. Issue #13 wires `MlWorkerClient` (HTTP to the FastAPI worker) and `GitHubService.postPrComment` into the flow, plus the controller-side `synchronize` re-review path (Issue #12 only handles opened/reopened; synchronize will need a separate PR-lookup-by-number branch). |
| 2026-06-21 | Issue #7 + #8 — Diff parser + sliding-window tokenizer. On branch `feat/issue-7-8-parser`. Added `apps/ml-worker/app/diff_parser.py` (class `FileHunk` with file_path/added_lines/removed_lines/language; `parse_diff()` handles unified diff, multi-file via `diff --git` headers, binary-file skip, empty-input `ValueError`; path extraction prefers `diff --git b/...` then `+++ b/...`; language map .py→python, .js/.ts→javascript, .java→java, else `unknown`; `hunks_to_text()` flattens for tokenization). Added `tests/test_diff_parser.py` with 8 tests including a real N+1 diff structure check. Added `apps/ml-worker/app/tokenizer_utils.py` (`sliding_window_tokenize()` encodes once then slices into overlapping 512-token windows with 50-token stride, returns list of `{input_ids, attention_mask}` dicts padded to max_length; `aggregate_logits()` max-pools per-window logits; both validate args and raise `ValueError` on bad input). Added `tests/test_tokenizer_utils.py` with a deterministic `FakeTokenizer` (no model load) and 8 tests. Added empty `tests/__init__.py`. 5 commits on the feature branch. Note: the spec spelled the dataclass `FilHunk` in one place and `FileHunk` in another — I went with `FileHunk` (the form used in the function signature), which is the conventional name; flag if the typo in the spec was intentional.

---

| 2026-06-22 | Issue #13 — Full review flow (continued). On branch `feat/issue-13-full-review-flow`. Three review-driven fixes plus unit tests: **(1)** `MlReviewRequest` now carries an explicit `mode` field (`diff`/`file`) and is propagated through to `MlWorkerService.review()`; `MlWorkerClient` constructs it explicitly. `ScanController.scanFile` passes `mode="file"`. **(2)** `GlobalExceptionHandler` cleaned up: dropped a 500-returning typo, kept `IllegalArgumentException → 400` and `EntityNotFoundException → 404` with proper JSON bodies (`error`, `message`, `status`, `timestamp`). **(3)** `ScanController.recordAction` now takes `@AuthenticationPrincipal UserDetails caller`, validates the action verb against a locked set (`accept`/`dismiss`/`fix`), enforces ownership via `isOwnedBy()` (finding → PR → repo.owner == caller.getUsername()), and returns 404 (not 403) to avoid leaking existence. New tests: **`ReviewServiceTest`** with 13 tests covering `detectLanguage` (python/java/javascript, .jsx/.ts extensions, default) and `computeQualityScore` (clamp +/−, fallback from findings, null safety); **`ScanControllerTest`** with 7 `@WebMvcTest` cases — `scanFile` happy path + 400 on blank + 400 on MlWorkerService rejection, `recordAction` 200 happy + 400 on invalid action + 404 on unknown finding + 404 on non-owner caller. **All 42 tests pass (`mvn test` BUILD SUCCESS).** Commit: `4380038` (5 files, 524 ins / 7 del). The pass-through `JwtAuthFilter` test override now sets a real `org.springframework.security.core.userdetails.User` as principal so `@AuthenticationPrincipal` resolves; the slice test imports `SecurityConfig` to keep the full security chain live.
| 2026-06-23 | Issues #14 + #15 — Repo management + API key auth. On branch `feat/issue-14-15`. **Both issues done in one session (they share the same SecurityConfig wiring).** New files: `entity/ApiKey.java` (`@Entity @Table(api_keys)`, columns id/label/prefix/key_hash/user_id/created_at/last_used_at/revoked, `revoked=false` default), `repository/ApiKeyRepository.java` (`findByPrefix`, `findAllByUserAndRevokedFalseOrderByCreatedAtDesc`, `revoke`), `service/ApiKeyService.java` (`create(user, label)` mints `cl_live_`+32 hex + BCrypt hash + 8-hex-char prefix; `findByPrefix`, `verify(stored, presented)` via `BCryptPasswordEncoder.matches`, `markUsed`, `revoke`; `KEY_PREFIX_LITERAL = "cl_live_"`); `service/RepoService.java` (full CRUD: `connect` (fullName parse → GitHub lookup → already-connected check → `generateSecret` (32 hex) → `gitHubService.installWebhook` → save with encrypted secret) all in one `@Transactional(rollbackFor=Exception.class)`; `disconnect` (soft-delete: GitHub webhook delete best-effort, set `active=false`); `listForOwner` (active repos + per-repo PR count + last reviewed + 30-day avg quality); `listPrs` (paginated); `qualityTrend` (date-bounded quality metrics); value types `ConnectResult(Repository, plaintextSecret)` and `RepoSummary(Repository, long, Instant, BigDecimal)`); `controller/RepoController.java` (5 endpoints: `POST /api/repos/connect` (201, returns `X-Webhook-Secret` header with the one-time plaintext), `GET /api/repos`, `GET /api/repos/{id}`, `DELETE /api/repos/{id}` (204), `GET /api/repos/{id}/prs?page=&size=` — all behind `@AuthenticationPrincipal User` and ownership-checked; 404 on cross-user access to avoid existence leaks); `controller/MetricsController.java` (`GET /api/metrics/quality-trend?repo=&days=30` returns `QualityTrendResponse(repoId, fullName, List<Point(date, avgQuality, prsReviewed, criticalCount, majorCount, minorCount)>, topAntiPattern)`; the top anti-pattern comes from `FindingRepository.findTopAntiPatterns(repo).findFirst()`); `controller/ApiKeyController.java` (`GET /api/auth/api-keys` lists caller's active keys with masked prefix; `POST /api/auth/api-keys` issues a new key (returns plaintext ONCE) and emits a new JWT cookie with `ROLE_API_KEY` claim so the user can test immediately; `DELETE /api/auth/api-keys/{id}` revokes); `security/ApiKeyAuthFilter.java` extends `OncePerRequestFilter`, gates ONLY `/api/scan/file` (`shouldNotFilter` returns true for everything else), falls through on missing/non-Bearer/non-`cl_live_` Authorization, otherwise looks up by prefix (literal+8hex) → BCrypt-verify → Redis `INCR ratelimit:apikey:{userId}` 60s sliding window → sets `ROLE_API_KEY` principal in `SecurityContextHolder`; 403/429 JSON bodies; `dto/request/{ConnectRepoRequest, CreateApiKeyRequest}.java` (Jakarta-validation records); `dto/response/{RepoResponse, ApiKeyResponse, CreatedApiKeyResponse, QualityTrendResponse}.java`; `exception/ConnectRepoException.java` (mapped to 400 by `GlobalExceptionHandler`); `db/migration/V3__api_keys.sql` (the `api_keys` table + indexes). Updated files: `SecurityConfig` now takes both `JwtAuthFilter` and `ApiKeyAuthFilter` as constructor args; `addFilterBefore(apiKeyAuthFilter, ...)` runs first so a key never falls through to JWT cookie validation; `/api/scan/file` requires `hasAnyRole("USER", "API_KEY")`; `application.yml` adds `app.ratelimit.api-key.requests-per-minute: 60` and `app.webhook.callback-url: ${CODELENS_API_URL}/api/webhook/github`; `GlobalExceptionHandler` maps `ConnectRepoException → 400` with `error: "CONNECT_REPO_FAILED"`; the auth controller's `/api-key/regenerate` was retired — replaced by `/api/auth/api-keys` CRUD. **Tests:** **`ApiKeyAuthFilterTest`** with 10 unit tests (no Spring context — direct filter calls with mocked `ApiKeyService` + `StringRedisTemplate`; covers pass-through on non-scan-file, pass-through on no/non-Bearer/non-prefix headers, 403 on unknown prefix, 403 on BCrypt mismatch, 403 on short key, 429 over rate-limit with `Retry-After: 60`, happy path sets `ROLE_API_KEY` principal and calls `redis.expire`, plus revoke-mark path). **`RepoControllerTest`** is `@SpringBootTest` (H2 in-memory, real Flyway V1+V3) with `GitHubService` + `ApiKeyAuthFilter` + `ApiKeyService` + `JwtAuthFilter` as `@MockBean`; uses `with(user(testUser))` request post-processor to seed the principal; stubs the filter mocks as pass-throughs via `doAnswer` so the security chain still runs authorization rules; covers connect happy path + invalid format + GitHub 404 + already-connected (idempotent guard) + list + get ownership + disconnect marks inactive + cross-user disconnect 404. Updated existing tests (`ScanControllerTest`, `ReviewControllerTest`, `WebhookControllerTest`) to stub the now-required `ApiKeyAuthFilter` mock as a pass-through via `doAnswer` (the mock's default `doFilter` returns null and would stall the security chain). Added `spring-security-test` test-scope dependency. **All 56 tests pass (mvn test BUILD SUCCESS; 14 new + 42 existing).** Commits: `feat(api): add ApiKey entity and V3 migration`, `feat(api): add ApiKeyService with bcrypt + prefix lookup`, `feat(api): add ApiKeyAuthFilter with Redis rate limit`, `feat(api): add ApiKeyController for key management`, `test(api): add ApiKeyAuthFilterTest with mocked Redis`, `feat(api): add RepoService with connect/disconnect/list logic`, `feat(api): add RepoController with 5 endpoints`, `feat(api): add MetricsController with quality-trend`, `test(api): add RepoControllerTest @SpringBootTest + H2`, `fix(api): wire ApiKeyAuthFilter into SecurityConfig`, `fix(api): add ConnectRepoException to GlobalExceptionHandler`, `docs: mark issues #14 and #15 complete`.

---

## 7. How to Use This File

- New session? Read this file first, then re-read [ENGINEERING_PLAN.md.md](./ENGINEERING_PLAN.md.md) Section relevant to your current issue.
- Starting an issue? Update the checkbox in Section 4.
- Making a non-trivial decision? Add it to Section 5 only after asking the principal engineer.
- Finished a session? Add a row to Section 6 with the date and a one-line summary.
